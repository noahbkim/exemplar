#include <cstdint>

int main()
{
    // What's the smallest type that fits the range 0 to 255?
    // TODO: replace `int` with the appropriate type.
    int x = 0;
    x = 255;

    // What integer type is signed and fits is 8 bytes wide?
    // TODO: replace `int` with the appropriate type.
    int y = -1;
}
